# RR Admin Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Перевести статическую секцию «От рендера до реальности» на `index.html` под управление админ-панели — редактируются заголовок, подзаголовок и произвольный список слайдов (render-фото, real-фото, название, подпись).

**Architecture:** Singleton-запись `rr_section` (title/subtitle) + таблица `rr_slides` (пара картинок + тексты + sort_order). Админка по паттерну `showroom` из `routes/admin.js` + `views/showroom/form.ejs`. Фронт: `GET /api/render-reality` → `js/render-vs-reality.js` сначала fetch, потом рендер слайдов, потом та же карусельная логика.

**Tech Stack:** Node.js / Express, Postgres через `pg`, EJS для админки, статический JS/HTML для фронта, multer + sharp для аплоадов.

**Test strategy:** В проекте нет тестового харнеса — используем ручную верификацию (migrate, curl, браузер) вместо юнит-тестов. Эти шаги выполняются против локальной БД (SSH-туннель на `:5433`, та же БД что и прод). Миграция идемпотентна, поэтому повторный прогон не ломает данные.

**Spec:** `docs/superpowers/specs/2026-04-22-rr-admin-design.md`

---

## Task 1: Миграция БД

**Files:**
- Create: `db/migrations/0002_render_reality.sql`

- [ ] **Step 1: Написать миграцию**

Создай `db/migrations/0002_render_reality.sql`:

```sql
-- Секция «От рендера до реальности»: singleton-настройки + список слайдов

CREATE TABLE IF NOT EXISTS rr_section (
  id         SERIAL PRIMARY KEY,
  title      VARCHAR(200) NOT NULL,
  subtitle   TEXT
);

CREATE TABLE IF NOT EXISTS rr_slides (
  id            SERIAL PRIMARY KEY,
  caption_title VARCHAR(200) NOT NULL,
  caption_meta  VARCHAR(300),
  render_image  VARCHAR(255) NOT NULL,
  real_image    VARCHAR(255) NOT NULL,
  sort_order    INT          DEFAULT 0,
  created_at    TIMESTAMP    DEFAULT NOW()
);

-- Сид: переносим текущий хардкод из index.html, только если секция ещё не настроена
INSERT INTO rr_section (title, subtitle)
SELECT
  E'ОТ РЕНДЕРА\nДО РЕАЛЬНОСТИ',
  'Каждый проект сначала живёт в 3D. Сравните, что обещали — и что построили.'
WHERE NOT EXISTS (SELECT 1 FROM rr_section);

INSERT INTO rr_slides (caption_title, caption_meta, render_image, real_image, sort_order)
SELECT * FROM (VALUES
  ('Бассейн с водопадом',  'Композитная чаша · бетонная скамья-фонтан',   'images/render-vs-reality/pair1-render.jpg', 'images/render-vs-reality/pair1-real.jpg', 1),
  ('Длинный лап-пул',      'Переливная система · подкатное укрытие',       'images/render-vs-reality/pair2-render.jpg', 'images/render-vs-reality/pair2-real.jpg', 2),
  ('Зона отдыха у воды',   'Натуральный камень · бирюзовая отделка чаши', 'images/render-vs-reality/pair3-render.jpg', 'images/render-vs-reality/pair3-real.jpg', 3)
) AS v(caption_title, caption_meta, render_image, real_image, sort_order)
WHERE NOT EXISTS (SELECT 1 FROM rr_slides);
```

- [ ] **Step 2: Прогнать миграцию локально (через туннель)**

Убедись что SSH-туннель поднят:

```bash
ssh -L 5433:localhost:5432 roman@95.163.236.186 -N &
```

Затем:

```bash
npm run migrate
```

Ожидаемо: `Applied: 0002_render_reality.sql` и `Applied 1 migration(s).`

- [ ] **Step 3: Проверить содержимое таблиц**

Через psql или DBeaver на `localhost:5433/good_pools_db`:

```sql
SELECT * FROM rr_section;
SELECT * FROM rr_slides ORDER BY sort_order;
```

Ожидаемо: 1 строка в `rr_section` с title (содержит перенос строки), 3 строки в `rr_slides`.

- [ ] **Step 4: Прогнать миграцию повторно — проверить идемпотентность**

```bash
npm run migrate
```

Ожидаемо: `No pending migrations.` (она уже в `schema_migrations`). Перепроверь что счётчики строк не изменились.

- [ ] **Step 5: Коммит**

```bash
git add db/migrations/0002_render_reality.sql
git commit -m "feat(db): add rr_section and rr_slides tables + seed"
```

---

## Task 2: Обновить `db/init.sql` для fresh install

**Files:**
- Modify: `db/init.sql`

- [ ] **Step 1: Добавить DDL и сид в init.sql**

В `db/init.sql` после блока `CREATE TABLE ... portfolio_images` (перед `admin_users`) вставить:

```sql
CREATE TABLE IF NOT EXISTS rr_section (
  id         SERIAL PRIMARY KEY,
  title      VARCHAR(200) NOT NULL,
  subtitle   TEXT
);

CREATE TABLE IF NOT EXISTS rr_slides (
  id            SERIAL PRIMARY KEY,
  caption_title VARCHAR(200) NOT NULL,
  caption_meta  VARCHAR(300),
  render_image  VARCHAR(255) NOT NULL,
  real_image    VARCHAR(255) NOT NULL,
  sort_order    INT          DEFAULT 0,
  created_at    TIMESTAMP    DEFAULT NOW()
);
```

В секцию сидов (после `Seed: portfolio`) добавить:

```sql
-- ============================================================
-- Seed: render-reality
-- ============================================================
INSERT INTO rr_section (title, subtitle)
SELECT
  E'ОТ РЕНДЕРА\nДО РЕАЛЬНОСТИ',
  'Каждый проект сначала живёт в 3D. Сравните, что обещали — и что построили.'
WHERE NOT EXISTS (SELECT 1 FROM rr_section);

INSERT INTO rr_slides (caption_title, caption_meta, render_image, real_image, sort_order)
SELECT * FROM (VALUES
  ('Бассейн с водопадом',  'Композитная чаша · бетонная скамья-фонтан',   'images/render-vs-reality/pair1-render.jpg', 'images/render-vs-reality/pair1-real.jpg', 1),
  ('Длинный лап-пул',      'Переливная система · подкатное укрытие',       'images/render-vs-reality/pair2-render.jpg', 'images/render-vs-reality/pair2-real.jpg', 2),
  ('Зона отдыха у воды',   'Натуральный камень · бирюзовая отделка чаши', 'images/render-vs-reality/pair3-render.jpg', 'images/render-vs-reality/pair3-real.jpg', 3)
) AS v(caption_title, caption_meta, render_image, real_image, sort_order)
WHERE NOT EXISTS (SELECT 1 FROM rr_slides);
```

- [ ] **Step 2: Коммит**

```bash
git add db/init.sql
git commit -m "feat(db): include rr_section/rr_slides in init.sql for fresh install"
```

---

## Task 3: API-эндпоинт `GET /api/render-reality`

**Files:**
- Modify: `routes/api.js`

- [ ] **Step 1: Добавить хендлер**

В `routes/api.js` перед `module.exports` вставить:

```javascript
// GET /api/render-reality
router.get('/render-reality', async function (req, res) {
  try {
    var section = await pool.query('SELECT title, subtitle FROM rr_section LIMIT 1');
    if (!section.rows.length) return res.json(null);

    var slides = await pool.query(
      'SELECT caption_title, caption_meta, render_image, real_image FROM rr_slides ORDER BY sort_order, id'
    );

    res.json({
      title: section.rows[0].title,
      subtitle: section.rows[0].subtitle,
      slides: slides.rows.map(function (s) {
        return {
          title: s.caption_title,
          meta: s.caption_meta,
          render: '/' + s.render_image,
          real: '/' + s.real_image
        };
      })
    });
  } catch (err) {
    console.error(err);
    res.status(500).json({ error: 'Server error' });
  }
});
```

- [ ] **Step 2: Запустить сервер и проверить curl**

```bash
npm run dev
```

В другом терминале:

```bash
curl -s http://localhost:3050/api/render-reality | node -e "let d=''; process.stdin.on('data',c=>d+=c).on('end',()=>console.log(JSON.stringify(JSON.parse(d),null,2)))"
```

Ожидаемо: JSON с `title` (содержит `\n`), `subtitle`, массив `slides` из 3 элементов. Пути у картинок начинаются со `/images/render-vs-reality/`.

- [ ] **Step 3: Коммит**

```bash
git add routes/api.js
git commit -m "feat(api): add GET /api/render-reality"
```

---

## Task 4: Admin-роуты для секции и CRUD слайдов

**Files:**
- Modify: `routes/admin.js`

- [ ] **Step 1: Добавить блок RR после блока Showroom**

В `routes/admin.js` после хендлера `router.post('/showroom/images/:imgId/delete', ...)` (перед `module.exports`) вставить:

```javascript
// ---------- Render vs Reality ----------

router.get('/render-reality', async function (req, res) {
  try {
    var section = await pool.query('SELECT * FROM rr_section LIMIT 1');
    var slides = await pool.query('SELECT * FROM rr_slides ORDER BY sort_order, id');
    renderAdmin(res, 'render-reality/form', {
      pageTitle: 'От рендера до реальности',
      active: 'render-reality',
      item: section.rows[0] || null,
      slides: slides.rows
    });
  } catch (err) {
    console.error(err);
    res.status(500).send('Server error');
  }
});

router.post('/render-reality', async function (req, res) {
  var { title, subtitle } = req.body;
  try {
    var existing = await pool.query('SELECT id FROM rr_section LIMIT 1');
    if (existing.rows.length) {
      await pool.query('UPDATE rr_section SET title=$1, subtitle=$2 WHERE id=$3',
        [title, subtitle || null, existing.rows[0].id]);
    } else {
      await pool.query('INSERT INTO rr_section (title, subtitle) VALUES ($1,$2)',
        [title, subtitle || null]);
    }
    req.session.success = 'Сохранено';
    res.redirect('/admin/render-reality');
  } catch (err) {
    console.error(err);
    req.session.error = 'Ошибка: ' + err.message;
    res.redirect('/admin/render-reality');
  }
});

// Создать слайд — оба файла обязательны
router.post('/render-reality/slides', ...processUpload('rr').fields([
  { name: 'render_image', maxCount: 1 },
  { name: 'real_image',   maxCount: 1 }
]), async function (req, res) {
  try {
    var { caption_title, caption_meta, sort_order } = req.body;
    var render = req.files && req.files.render_image && req.files.render_image[0];
    var real   = req.files && req.files.real_image   && req.files.real_image[0];
    if (!render || !real) {
      req.session.error = 'Нужны обе картинки: render и real';
      return res.redirect('/admin/render-reality');
    }
    await pool.query(
      'INSERT INTO rr_slides (caption_title, caption_meta, render_image, real_image, sort_order) VALUES ($1,$2,$3,$4,$5)',
      [caption_title, caption_meta || null, 'uploads/rr/' + render.filename, 'uploads/rr/' + real.filename, parseInt(sort_order) || 0]
    );
    req.session.success = 'Слайд добавлен';
    res.redirect('/admin/render-reality');
  } catch (err) {
    console.error(err);
    req.session.error = 'Ошибка: ' + err.message;
    res.redirect('/admin/render-reality');
  }
});

// Обновить слайд — картинки опциональны
router.post('/render-reality/slides/:id', ...processUpload('rr').fields([
  { name: 'render_image', maxCount: 1 },
  { name: 'real_image',   maxCount: 1 }
]), async function (req, res) {
  try {
    var { caption_title, caption_meta, sort_order } = req.body;
    var existing = await pool.query('SELECT render_image, real_image FROM rr_slides WHERE id=$1', [req.params.id]);
    if (!existing.rows.length) {
      req.session.error = 'Слайд не найден';
      return res.redirect('/admin/render-reality');
    }
    var old = existing.rows[0];
    var newRender = req.files && req.files.render_image && req.files.render_image[0];
    var newReal   = req.files && req.files.real_image   && req.files.real_image[0];

    var renderPath = old.render_image;
    var realPath   = old.real_image;

    if (newRender) {
      deleteFile(old.render_image);
      renderPath = 'uploads/rr/' + newRender.filename;
    }
    if (newReal) {
      deleteFile(old.real_image);
      realPath = 'uploads/rr/' + newReal.filename;
    }

    await pool.query(
      'UPDATE rr_slides SET caption_title=$1, caption_meta=$2, render_image=$3, real_image=$4, sort_order=$5 WHERE id=$6',
      [caption_title, caption_meta || null, renderPath, realPath, parseInt(sort_order) || 0, req.params.id]
    );
    req.session.success = 'Слайд обновлён';
    res.redirect('/admin/render-reality');
  } catch (err) {
    console.error(err);
    req.session.error = 'Ошибка: ' + err.message;
    res.redirect('/admin/render-reality');
  }
});

router.post('/render-reality/slides/:id/delete', async function (req, res) {
  try {
    var result = await pool.query('DELETE FROM rr_slides WHERE id=$1 RETURNING render_image, real_image', [req.params.id]);
    if (result.rows.length) {
      deleteFile(result.rows[0].render_image);
      deleteFile(result.rows[0].real_image);
    }
    req.session.success = 'Слайд удалён';
    res.redirect('/admin/render-reality');
  } catch (err) {
    console.error(err);
    req.session.error = 'Ошибка удаления';
    res.redirect('/admin/render-reality');
  }
});
```

Обрати внимание:
- `processUpload('rr')` автоматически создаст `uploads/rr/` — в `middleware/upload.js` есть `fs.mkdirSync(dest, { recursive: true })`.
- `deleteFile()` принимает **относительный** путь от корня проекта. Для сид-картинок путь вида `images/render-vs-reality/pair1-render.jpg` резолвится в реальный файл в `images/`. Это ок: удаление происходит только когда пользователь явно заменяет фото (upload нового файла) — и после замены DB-запись на старый путь не ссылается, поэтому удалить старый файл безопасно. При чистом редактировании текста (без upload) `deleteFile()` не вызывается.

- [ ] **Step 2: Перезапустить сервер, проверить GET**

```bash
# сервер уже запущен через npm run dev — он перезапустится автоматически
curl -i http://localhost:3050/admin/render-reality
```

Ожидаемо: `HTTP/1.1 302 Found` → `Location: /admin/login` (т.к. без сессии). Это валидный ответ, значит роут смонтирован.

- [ ] **Step 3: Коммит**

```bash
git add routes/admin.js
git commit -m "feat(admin): add render-reality singleton + slides CRUD routes"
```

---

## Task 5: Админ-view `views/render-reality/form.ejs`

**Files:**
- Create: `views/render-reality/form.ejs`

- [ ] **Step 1: Создать view**

```ejs
<h1>От рендера до реальности</h1>

<form method="POST" action="/admin/render-reality" style="max-width:600px;">
  <div class="form-group">
    <label for="title">Заголовок</label>
    <textarea id="title" name="title" rows="2" required style="resize:vertical;"><%= item ? item.title : '' %></textarea>
    <small style="color:#71717a; font-size:12px;">Перенос строки (Enter) делит заголовок на bold/thin на сайте.</small>
  </div>

  <div class="form-group">
    <label for="subtitle">Подзаголовок</label>
    <textarea id="subtitle" name="subtitle" rows="3"><%= item ? item.subtitle || '' : '' %></textarea>
  </div>

  <div class="form-actions">
    <button type="submit" class="btn btn-primary">Сохранить</button>
  </div>
</form>

<hr style="border-color:#27272a; margin: 32px 0;">
<h2 style="font-size:18px; margin-bottom:16px;">Слайды</h2>

<% slides.forEach(function(s) { %>
  <div style="background:#1a1b23; border:1px solid #27272a; border-radius:12px; padding:16px; margin-bottom:16px;">
    <form method="POST" action="/admin/render-reality/slides/<%= s.id %>" enctype="multipart/form-data">
      <div style="display:flex; gap:16px; margin-bottom:12px;">
        <div style="flex:0 0 auto;">
          <div style="font-size:12px; color:#a1a1aa; margin-bottom:4px;">Рендер</div>
          <img src="/<%= s.render_image %>" alt="" style="width:160px; height:100px; object-fit:cover; border-radius:8px; display:block; margin-bottom:6px;">
          <input type="file" name="render_image" accept=".jpg,.jpeg,.png,.webp" style="font-size:12px;">
        </div>
        <div style="flex:0 0 auto;">
          <div style="font-size:12px; color:#a1a1aa; margin-bottom:4px;">Реальность</div>
          <img src="/<%= s.real_image %>" alt="" style="width:160px; height:100px; object-fit:cover; border-radius:8px; display:block; margin-bottom:6px;">
          <input type="file" name="real_image" accept=".jpg,.jpeg,.png,.webp" style="font-size:12px;">
        </div>
      </div>

      <div class="form-row">
        <div class="form-group">
          <label>Название</label>
          <input type="text" name="caption_title" required value="<%= s.caption_title %>">
        </div>
        <div class="form-group">
          <label>Подпись</label>
          <input type="text" name="caption_meta" value="<%= s.caption_meta || '' %>">
        </div>
        <div class="form-group" style="flex:0 0 100px;">
          <label>Порядок</label>
          <input type="number" name="sort_order" value="<%= s.sort_order %>">
        </div>
      </div>

      <div style="display:flex; gap:8px;">
        <button type="submit" class="btn btn-primary btn-sm">Сохранить</button>
      </div>
    </form>
    <form method="POST" action="/admin/render-reality/slides/<%= s.id %>/delete" onsubmit="return confirm('Удалить слайд?')" style="display:inline; margin-top:8px;">
      <button type="submit" class="btn btn-danger btn-sm">Удалить слайд</button>
    </form>
  </div>
<% }); %>

<div style="background:#1a1b23; border:1px dashed #3f3f46; border-radius:12px; padding:16px; margin-top:24px;">
  <div style="font-size:16px; margin-bottom:12px;">Добавить новый слайд</div>
  <form method="POST" action="/admin/render-reality/slides" enctype="multipart/form-data">
    <div style="display:flex; gap:16px; margin-bottom:12px;">
      <div>
        <div style="font-size:12px; color:#a1a1aa; margin-bottom:4px;">Рендер *</div>
        <input type="file" name="render_image" accept=".jpg,.jpeg,.png,.webp" required>
      </div>
      <div>
        <div style="font-size:12px; color:#a1a1aa; margin-bottom:4px;">Реальность *</div>
        <input type="file" name="real_image" accept=".jpg,.jpeg,.png,.webp" required>
      </div>
    </div>

    <div class="form-row">
      <div class="form-group">
        <label>Название</label>
        <input type="text" name="caption_title" required placeholder="Бассейн с водопадом">
      </div>
      <div class="form-group">
        <label>Подпись</label>
        <input type="text" name="caption_meta" placeholder="Переливная система · подкатное укрытие">
      </div>
      <div class="form-group" style="flex:0 0 100px;">
        <label>Порядок</label>
        <input type="number" name="sort_order" value="0">
      </div>
    </div>

    <button type="submit" class="btn btn-primary btn-sm">Добавить слайд</button>
  </form>
</div>
```

- [ ] **Step 2: Коммит**

```bash
git add views/render-reality/form.ejs
git commit -m "feat(admin): add render-reality admin view"
```

---

## Task 6: Ссылка в сайдбаре

**Files:**
- Modify: `views/layout.ejs`

- [ ] **Step 1: Добавить пункт в sidebar**

В `views/layout.ejs` после строки с `<a href="/admin/showroom" ...>Площадка</a>` добавить:

```ejs
<a href="/admin/render-reality" class="<%= typeof active !== 'undefined' && active === 'render-reality' ? 'active' : '' %>">Рендер↔Реальность</a>
```

- [ ] **Step 2: Проверить в браузере**

Зайди на `http://localhost:3050/admin` (после логина). В сайдбаре должна появиться ссылка. Клик → открывается страница со списком 3 слайдов.

Проверь: 
- Заголовок в `<textarea>` содержит перенос строки.
- Каждый слайд показывает обе миниатюры.
- Кнопки «Сохранить» и «Удалить слайд» работают (удаление с confirm).

Попробуй:
- Отредактировать заголовок (сохраняется, ставится toast «Сохранено»).
- Обновить один слайд, заменив только текст (картинки не должны перезагрузиться).
- Обновить один слайд с новой картинкой (старая удаляется из `uploads/rr/` — файл сид-картинок в `images/render-vs-reality/` не трогаем вручную; старые `pair*-render.jpg` останутся на диске, это ок).
- Добавить новый слайд (2 файла + тексты) — появляется в списке.
- Удалить слайд — подтверждение → исчезает, файлы из `uploads/rr/` удаляются.

- [ ] **Step 3: Коммит**

```bash
git add views/layout.ejs
git commit -m "feat(admin): add render-reality sidebar link"
```

---

## Task 7: Убрать хардкод из `index.html`

**Files:**
- Modify: `index.html:168-233`

- [ ] **Step 1: Заменить содержимое секции**

В `index.html` найди секцию `<section class="rr" id="render-reality">`. Замени **содержимое `.rr-head`** и **содержимое `.rr-track`** на пустые контейнеры с комментариями (eyebrow, кнопка, toggle, стрелки, dots — остаются на месте).

Было (строки ~170-176):
```html
<div class="rr-head">
  <div class="rr-eyebrow">Визуализация · реализация</div>
  <h2 class="rr-title">
    <span class="bold">ОТ РЕНДЕРА</span><br>
    <span class="thin">ДО РЕАЛЬНОСТИ</span>
  </h2>
  <p class="rr-sub">Каждый проект сначала живёт в 3D. Сравните, что обещали — и что построили.</p>
  <a href="https://pool-builder-crm.ru/portfolio" target="_blank" rel="noopener" class="btn-all-top">Смотреть все рендеры <span class="arrow">&rarr;</span></a>
</div>
```

Станет:
```html
<div class="rr-head">
  <div class="rr-eyebrow">Визуализация · реализация</div>
  <h2 class="rr-title"><!-- рендерится из js/render-vs-reality.js --></h2>
  <p class="rr-sub"><!-- рендерится из js/render-vs-reality.js --></p>
  <a href="https://pool-builder-crm.ru/portfolio" target="_blank" rel="noopener" class="btn-all-top">Смотреть все рендеры <span class="arrow">&rarr;</span></a>
</div>
```

Было (строки ~191-228):
```html
<div class="rr-track">
  <div class="rr-slide is-active" data-index="0"> ... </div>
  <div class="rr-slide" data-index="1"> ... </div>
  <div class="rr-slide" data-index="2"> ... </div>
</div>
```

Станет:
```html
<div class="rr-track"><!-- слайды рендерятся из js/render-vs-reality.js --></div>
```

- [ ] **Step 2: Обновить cache-bust query для `render-vs-reality.js`**

В `index.html` найди подключение скрипта:
```html
<script src="js/render-vs-reality.js?v=..."></script>
```
Увеличить версию (например, заменить текущее значение на сегодняшнюю дату `?v=20260422`).

- [ ] **Step 3: Коммит**

```bash
git add index.html
git commit -m "refactor(index): remove hardcoded rr content, JS will render it"
```

---

## Task 8: Рефакторинг `js/render-vs-reality.js` — fetch + render + carousel

**Files:**
- Modify: `js/render-vs-reality.js` (полная замена)

- [ ] **Step 1: Переписать файл**

Замени всё содержимое `js/render-vs-reality.js` на:

```javascript
(function () {
  document.addEventListener('DOMContentLoaded', function () {
    var section = document.getElementById('render-reality');
    if (!section) return;

    init(section).catch(function (err) { console.error('RR init failed:', err); section.hidden = true; });
  });

  async function init(section) {
    var data = null;
    try {
      var res = await fetch('/api/render-reality');
      if (res.ok) data = await res.json();
    } catch (e) { /* noop */ }

    if (!data || !data.slides || !data.slides.length) {
      section.hidden = true;
      return;
    }

    renderHead(section, data);
    renderSlides(section, data.slides);
    initCarousel(section);
  }

  function renderHead(section, data) {
    var titleEl = section.querySelector('.rr-title');
    var subEl   = section.querySelector('.rr-sub');

    var lines = (data.title || '').split('\n');
    var boldPart = lines[0] || '';
    var thinPart = lines.slice(1).join(' ');

    titleEl.innerHTML = '';
    var bold = document.createElement('span');
    bold.className = 'bold';
    bold.textContent = boldPart;
    titleEl.appendChild(bold);
    if (thinPart) {
      titleEl.appendChild(document.createElement('br'));
      var thin = document.createElement('span');
      thin.className = 'thin';
      thin.textContent = thinPart;
      titleEl.appendChild(thin);
    }

    subEl.textContent = data.subtitle || '';
  }

  function renderSlides(section, slides) {
    var track = section.querySelector('.rr-track');
    track.innerHTML = '';

    slides.forEach(function (s, i) {
      var slide = document.createElement('div');
      slide.className = 'rr-slide' + (i === 0 ? ' is-active' : '');
      slide.dataset.index = String(i);

      var frame = document.createElement('div');
      frame.className = 'rr-frame';

      var imgR = document.createElement('img');
      imgR.className = 'rr-img rr-img-render';
      imgR.src = s.render;
      imgR.alt = '3D-визуализация: ' + (s.title || '');
      if (i !== 0) imgR.loading = 'lazy';

      var imgReal = document.createElement('img');
      imgReal.className = 'rr-img rr-img-real';
      imgReal.src = s.real;
      imgReal.alt = 'Реальная фотография: ' + (s.title || '');
      imgReal.loading = 'lazy';

      var badgeR = document.createElement('span');
      badgeR.className = 'rr-badge rr-badge-render';
      badgeR.textContent = 'Рендер';
      var badgeReal = document.createElement('span');
      badgeReal.className = 'rr-badge rr-badge-real';
      badgeReal.textContent = 'Реальность';

      frame.appendChild(imgR);
      frame.appendChild(imgReal);
      frame.appendChild(badgeR);
      frame.appendChild(badgeReal);

      var caption = document.createElement('div');
      caption.className = 'rr-caption';
      var capTitle = document.createElement('div');
      capTitle.className = 'rr-caption-title';
      capTitle.textContent = s.title || '';
      caption.appendChild(capTitle);
      if (s.meta) {
        var capMeta = document.createElement('div');
        capMeta.className = 'rr-caption-meta';
        capMeta.textContent = s.meta;
        caption.appendChild(capMeta);
      }

      slide.appendChild(frame);
      slide.appendChild(caption);
      track.appendChild(slide);
    });
  }

  function initCarousel(section) {
    var toggle   = section.querySelector('.rr-toggle');
    var tabs     = section.querySelectorAll('.rr-tab');
    var stage    = section.querySelector('.rr-stage');
    var track    = section.querySelector('.rr-track');
    var slides   = section.querySelectorAll('.rr-slide');
    var prev     = section.querySelector('.rr-prev');
    var next     = section.querySelector('.rr-next');
    var dotsHost = section.querySelector('.rr-dots');
    var viewport = section.querySelector('.rr-viewport');

    var index = 0;
    var total = slides.length;

    function setMode(mode) {
      stage.dataset.mode = mode;
      toggle.dataset.mode = mode;
      tabs.forEach(function (t) {
        var active = t.dataset.mode === mode;
        t.classList.toggle('is-active', active);
        t.setAttribute('aria-selected', active ? 'true' : 'false');
      });
    }

    function goTo(i) {
      index = (i + total) % total;
      track.style.transform = 'translateX(-' + (index * 100) + '%)';
      slides.forEach(function (s, k) { s.classList.toggle('is-active', k === index); });
      dotsHost.querySelectorAll('.rr-dot').forEach(function (d, k) { d.classList.toggle('is-active', k === index); });
      if (prev) prev.disabled = false;
      if (next) next.disabled = false;
    }

    tabs.forEach(function (tab) {
      tab.addEventListener('click', function () { setMode(tab.dataset.mode); });
    });

    if (prev) prev.addEventListener('click', function () { goTo(index - 1); });
    if (next) next.addEventListener('click', function () { goTo(index + 1); });

    dotsHost.innerHTML = '';
    for (var i = 0; i < total; i++) {
      (function (idx) {
        var dot = document.createElement('button');
        dot.type = 'button';
        dot.className = 'rr-dot' + (idx === 0 ? ' is-active' : '');
        dot.setAttribute('aria-label', 'Проект ' + (idx + 1));
        dot.addEventListener('click', function () { goTo(idx); });
        dotsHost.appendChild(dot);
      })(i);
    }

    var startX = 0, dx = 0, dragging = false;

    viewport.addEventListener('pointerdown', function (e) {
      if (e.pointerType === 'mouse' && e.button !== 0) return;
      dragging = true;
      startX = e.clientX;
      dx = 0;
      viewport.setPointerCapture(e.pointerId);
    });
    viewport.addEventListener('pointermove', function (e) {
      if (!dragging) return;
      dx = e.clientX - startX;
    });
    viewport.addEventListener('pointerup', function () {
      if (!dragging) return;
      dragging = false;
      if (Math.abs(dx) > 40) {
        goTo(index + (dx < 0 ? 1 : -1));
      }
    });
    viewport.addEventListener('pointercancel', function () { dragging = false; });

    section.addEventListener('keydown', function (e) {
      if (e.key === 'ArrowLeft') goTo(index - 1);
      else if (e.key === 'ArrowRight') goTo(index + 1);
    });
  }
})();
```

Важно: функция `initCarousel` делает `section.querySelectorAll('.rr-slide')` **после** `renderSlides`, поэтому NodeList содержит свежесгенерированные слайды.

- [ ] **Step 2: Проверить в браузере**

Открой `http://localhost:3050/`, прокрути до секции «От рендера до реальности». Должно работать как до — заголовок (bold/thin), подзаголовок, карусель из 3 слайдов, toggle Рендер↔Реальность, стрелки, точки, свайп.

Открой DevTools → Network → фильтр `render-reality`: должен быть один запрос `/api/render-reality`, ответ 200.

- [ ] **Step 3: Edge case — пустая БД**

Временно удали все слайды из `rr_slides`:
```sql
DELETE FROM rr_slides;
```
(Или через админку — удали все три кнопкой «Удалить слайд».)

Обнови главную страницу. Секция `#render-reality` должна быть скрыта (`hidden` атрибут). Страница не падает.

Восстанови слайды (перезапусти миграцию вручную, или добавь через админку).

- [ ] **Step 4: Коммит**

```bash
git add js/render-vs-reality.js
git commit -m "refactor(rr): fetch slides from /api/render-reality and render dynamically"
```

---

## Task 9: Сквозная проверка

- [ ] **Step 1: Отредактировать заголовок/подзаголовок и слайд через админку**

1. Открой `http://localhost:3050/admin/render-reality`.
2. Измени заголовок (например, `ДО РЕАЛЬНОСТИ` → `ДО ВОПЛОЩЕНИЯ`), сохрани.
3. Обнови главную: заголовок секции изменился (thin-часть).
4. Отредактируй первый слайд: поменяй подпись на что-то новое.
5. Обнови главную: подпись под первым слайдом изменилась.

- [ ] **Step 2: Добавить новый слайд**

В админке → «Добавить новый слайд» → загрузи 2 файла (любые render + real jpg), заполни название и подпись, sort_order = 99, submit. Проверь главную: в карусели теперь 4 точки, новый слайд последний.

- [ ] **Step 3: Удалить добавленный слайд**

В админке → «Удалить слайд» для добавленного → confirm. Файлы из `uploads/rr/` исчезают (можно проверить `ls uploads/rr/` на сервере — но локально папка на Windows). Главная: снова 3 точки.

- [ ] **Step 4: Восстановить заголовок к исходному**

Через админку верни title обратно на `ОТ РЕНДЕРА\nДО РЕАЛЬНОСТИ`.

- [ ] **Step 5: Push**

```bash
git push origin main
```

Деплой автоматически:
- rsync бэкенда и фронта,
- `npm install --omit=dev`,
- `node db/migrate.js` — применит `0002_render_reality.sql` на сервере,
- `pm2 restart good-pools`.

Через 1-2 минуты открой продакшен-сайт и проверь секцию визуально + админку по адресу боевого `/admin/render-reality`.

---

## Файлы (итого)

**Новые:**
- `db/migrations/0002_render_reality.sql`
- `views/render-reality/form.ejs`
- `uploads/rr/` (создастся автоматически при первой загрузке)

**Изменённые:**
- `db/init.sql`
- `routes/api.js`
- `routes/admin.js`
- `views/layout.ejs`
- `index.html`
- `js/render-vs-reality.js`
