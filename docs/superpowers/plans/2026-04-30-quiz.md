# Quiz «Рассчитать стоимость» Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** На главной странице добавить модальный 7-шаговый квиз «Рассчитать стоимость» 1:1 по содержанию с референсом `xn--80ablclk2abatqa7b6b2c.рф`, открывающийся по клику на hero-кнопку. Только фронтенд: submit логирует в console + показывает «спасибо», без бэкенда.

**Architecture:** Vanilla JS (IIFE, как остальные `js/*.js` в проекте) + нативный `<dialog>`. Один модуль `js/quiz.js` со state-машиной по шагам, конфиг шагов в массиве `STEPS`, рендер через шаблонные строки и `innerHTML` контейнера `.quiz-body`. Чистые функции (`validateStep`, `normalizePhone`) экспортированы через `window.__quiz` для теста в `js/quiz.test.html`. Стили в отдельном `css/quiz.css`.

**Tech Stack:** Vanilla JS (без сборки, без модулей), CSS, нативный `<dialog>`, `console.assert` для тестов.

**Spec:** `docs/superpowers/specs/2026-04-30-quiz-design.md`

---

## File Structure

**Create:**
- `js/quiz.js` — модуль квиза (IIFE).
- `js/quiz.test.html` — мини-набор тестов через `console.assert` для чистых функций.
- `css/quiz.css` — стили модалки и шагов.

**Modify:**
- `index.html` — подключить `quiz.css` в `<head>`, `quiz.js` в подвале со скриптами, заменить тег hero-кнопки на `<button data-quiz-open>`.

**Не трогать:** `js/service.js`, форма `#serviceForm` в `index.html` (`#service` секция остаётся как есть), серверная часть, БД.

---

## Task 1: Чистая функция `normalizePhone` + первый тест

**Files:**
- Create: `js/quiz.js`
- Create: `js/quiz.test.html`

- [ ] **Step 1: Создать `js/quiz.test.html` с падающим тестом**

```html
<!DOCTYPE html>
<html lang="ru">
<head><meta charset="UTF-8"><title>quiz tests</title></head>
<body>
<h1>Quiz tests — открой DevTools → Console</h1>
<script src="quiz.js"></script>
<script>
  (function () {
    let pass = 0, fail = 0;
    function eq(actual, expected, name) {
      if (actual === expected) { pass++; console.log('OK:', name); }
      else { fail++; console.error('FAIL:', name, '\n  expected:', JSON.stringify(expected), '\n  actual:', JSON.stringify(actual)); }
    }
    const Q = window.__quiz;

    // normalizePhone
    eq(Q.normalizePhone('+7 (123) 456-78-90'), '71234567890', 'normalizePhone strips non-digits');
    eq(Q.normalizePhone(''),                   '',            'normalizePhone empty string');
    eq(Q.normalizePhone('abc'),                '',            'normalizePhone non-numeric');
    eq(Q.normalizePhone('  +7 999 \n123-45-67'),'79991234567','normalizePhone whitespace + symbols');
    eq(Q.normalizePhone(null),                 '',            'normalizePhone null safe');
    eq(Q.normalizePhone(undefined),            '',            'normalizePhone undefined safe');

    console.log(`---\nResult: ${pass} passed, ${fail} failed`);
  })();
</script>
</body>
</html>
```

- [ ] **Step 2: Создать `js/quiz.js` со скелетом без функции**

```js
(function () {
  'use strict';

  window.__quiz = {};
})();
```

- [ ] **Step 3: Открыть тест и убедиться, что он падает**

Открыть в браузере: `http://localhost:3050/js/quiz.test.html` (dev-сервер уже запущен на 3050).
Ожидаемое: в консоли 6 строк `FAIL: ...` (TypeError: undefined is not a function), внизу `Result: 0 passed, 6 failed`.

- [ ] **Step 4: Реализовать `normalizePhone` в `js/quiz.js`**

```js
(function () {
  'use strict';

  function normalizePhone(raw) {
    if (raw == null) return '';
    return String(raw).replace(/\D/g, '');
  }

  window.__quiz = { normalizePhone };
})();
```

- [ ] **Step 5: Перезагрузить тест, убедиться что зелёный**

Перезагрузить `quiz.test.html` в браузере.
Ожидаемое: 6 строк `OK: ...`, внизу `Result: 6 passed, 0 failed`.

- [ ] **Step 6: Коммит**

```bash
git add js/quiz.js js/quiz.test.html
git commit -m "feat(quiz): add normalizePhone helper with tests"
```

---

## Task 2: Чистая функция `validateStep` + тесты

**Files:**
- Modify: `js/quiz.js`
- Modify: `js/quiz.test.html`

- [ ] **Step 1: Дописать ассерты в `quiz.test.html`** (после блока `normalizePhone`, до `console.log('---...')`)

```js
    // validateStep
    const v = (s, a) => Q.validateStep(s, a);
    eq(v(1, { size: '3x5' }).ok,       true,  'step 1 with size → ok');
    eq(v(1, { size: null }).ok,        false, 'step 1 without size → not ok');
    eq(v(1, { size: null }).message,   'Выберите размер', 'step 1 error message');
    eq(v(2, { finish: 'composite' }).ok, true, 'step 2 with finish → ok');
    eq(v(2, { finish: null }).ok,      false, 'step 2 without finish → not ok');
    eq(v(2, { finish: null }).message, 'Выберите тип отделки', 'step 2 error message');
    eq(v(3, { filtration: 'yes' }).ok, true,  'step 3 with filtration → ok');
    eq(v(3, {}).ok,                    false, 'step 3 without filtration → not ok');
    eq(v(4, { options: [] }).ok,       true,  'step 4 empty multi → ok (optional)');
    eq(v(4, { options: ['heating'] }).ok, true, 'step 4 with selection → ok');
    eq(v(5, { budget: '1-2' }).ok,     true,  'step 5 with budget → ok');
    eq(v(5, {}).ok,                    false, 'step 5 without budget → not ok');
    eq(v(6, { timing: 'soon' }).ok,    true,  'step 6 with timing → ok');
    eq(v(6, {}).ok,                    false, 'step 6 without timing → not ok');
    eq(v(7, { name: 'Ив',  phone: '79991234567' }).ok, true,  'step 7 valid');
    eq(v(7, { name: 'И',   phone: '79991234567' }).ok, false, 'step 7 short name → not ok');
    eq(v(7, { name: 'И',   phone: '79991234567' }).message, 'Введите имя', 'step 7 name error');
    eq(v(7, { name: 'Ив',  phone: '123' }).ok,         false, 'step 7 short phone → not ok');
    eq(v(7, { name: 'Ив',  phone: '123' }).message,    'Введите телефон', 'step 7 phone error');
    eq(v(7, { name: '   Ив   ', phone: '+7 (999) 123-45-67' }).ok, true, 'step 7 trims/normalizes');
```

- [ ] **Step 2: Перезагрузить тест, убедиться что новые тесты падают**

Перезагрузить `quiz.test.html`.
Ожидаемое: 6 OK (от `normalizePhone`) + 20 FAIL (TypeError: undefined is not a function).

- [ ] **Step 3: Реализовать `validateStep` в `js/quiz.js`**

Добавить функцию **внутри IIFE до строки `window.__quiz = ...`** и обновить экспорт:

```js
  function validateStep(step, answers) {
    switch (step) {
      case 1:
        if (!answers.size) return { ok: false, message: 'Выберите размер' };
        return { ok: true };
      case 2:
        if (!answers.finish) return { ok: false, message: 'Выберите тип отделки' };
        return { ok: true };
      case 3:
        if (!answers.filtration) return { ok: false, message: 'Выберите вариант' };
        return { ok: true };
      case 4:
        return { ok: true };
      case 5:
        if (!answers.budget) return { ok: false, message: 'Выберите диапазон' };
        return { ok: true };
      case 6:
        if (!answers.timing) return { ok: false, message: 'Выберите срок' };
        return { ok: true };
      case 7: {
        const name = String(answers.name || '').trim();
        if (name.length < 2) return { ok: false, message: 'Введите имя' };
        const digits = normalizePhone(answers.phone);
        if (digits.length < 10) return { ok: false, message: 'Введите телефон' };
        return { ok: true };
      }
      default:
        return { ok: false, message: 'Unknown step' };
    }
  }

  window.__quiz = { normalizePhone, validateStep };
```

- [ ] **Step 4: Перезагрузить тест, всё зелёное**

Ожидаемое: `Result: 26 passed, 0 failed`.

- [ ] **Step 5: Коммит**

```bash
git add js/quiz.js js/quiz.test.html
git commit -m "feat(quiz): add validateStep with per-step rules and tests"
```

---

## Task 3: Конфиг шагов `STEPS` и стейт

**Files:**
- Modify: `js/quiz.js`

- [ ] **Step 1: Добавить `STEPS`, `TOTAL_STEPS`, `makeInitialState`, `state` в IIFE**

Вставить **сразу после `'use strict';`**, до объявления `normalizePhone`:

```js
  const STEPS = [
    { id: 'size', title: 'Размер бассейна', type: 'single', options: [
      { value: '3x5',    label: '3 × 5 м' },
      { value: '3x6',    label: '3 × 6 м' },
      { value: '3x7',    label: '3 × 7 м' },
      { value: '4x8',    label: '4 × 8 м' },
      { value: '5x10',   label: '5 × 10 м' },
      { value: 'custom', label: 'Мне нужен индивидуальный размер' }
    ]},
    { id: 'finish', title: 'Тип отделки', type: 'single', options: [
      { value: 'composite', label: 'Композитный' },
      { value: 'concrete',  label: 'Бетонный' },
      { value: 'consult',   label: 'Не знаю, нужна консультация' }
    ]},
    { id: 'filtration', title: 'Фильтрация воды', type: 'single', options: [
      { value: 'yes',     label: 'Да' },
      { value: 'consult', label: 'Нет, нужна консультация' }
    ]},
    { id: 'options', title: 'Дополнительные опции', type: 'multi', options: [
      { value: 'heating',      label: 'Подогрев воды' },
      { value: 'lighting',     label: 'Подсветка' },
      { value: 'autodose',     label: 'Автодозирование химии' },
      { value: 'uv',           label: 'УФ лампа' },
      { value: 'chlorinefree', label: 'Безхлорная система' },
      { value: 'hydromassage', label: 'Гидромассаж / Аэромассаж' },
      { value: 'waterfall',    label: 'Водопад' }
    ]},
    { id: 'budget', title: 'Бюджет', type: 'single', options: [
      { value: '1-2', label: '1–2 млн руб' },
      { value: '2-3', label: '2–3 млн руб' },
      { value: '3-4', label: '3–4 млн руб' }
    ]},
    { id: 'timing', title: 'Сроки', type: 'single', options: [
      { value: 'soon',     label: 'В ближайшее время' },
      { value: 'season',   label: 'В тёплый сезон' },
      { value: 'browsing', label: 'Пока просто прицениваюсь' }
    ]},
    { id: 'contacts', title: 'Контакты', type: 'contacts' }
  ];

  const TOTAL_STEPS = STEPS.length;

  function makeInitialState() {
    return {
      step: 1,
      answers: {
        size: null,
        finish: null,
        filtration: null,
        options: [],
        budget: null,
        timing: null,
        name: '',
        phone: ''
      }
    };
  }

  let state = makeInitialState();
```

- [ ] **Step 2: Убедиться, что тесты по-прежнему зелёные**

Перезагрузить `quiz.test.html`.
Ожидаемое: `Result: 26 passed, 0 failed` (ничего не сломали).

- [ ] **Step 3: Коммит**

```bash
git add js/quiz.js
git commit -m "feat(quiz): add STEPS config and initial state"
```

---

## Task 4: Подключить `quiz.js`/`quiz.css` и переделать hero-кнопку

**Files:**
- Modify: `index.html`
- Create: `css/quiz.css` (пустой файл-заглушка, чтобы `<link>` не дал 404)

- [ ] **Step 1: Создать пустой `css/quiz.css`**

```css
/* css/quiz.css — будет наполнен в Task 5+ */
```

- [ ] **Step 2: Подключить CSS в `index.html`**

В `<head>`, **сразу после** строки `<link rel="stylesheet" href="css/style.css?v=20260429-rr1100">` (строка 7), добавить:

```html
  <link rel="stylesheet" href="css/quiz.css?v=20260430">
```

- [ ] **Step 3: Подключить JS в `index.html`**

В подвале со скриптами (строки ~451–457), **сразу после** последней строки `<script src="js/render-vs-reality.js?v=20260422"></script>`, добавить:

```html
  <script src="js/quiz.js?v=20260430"></script>
```

- [ ] **Step 4: Заменить тег hero-кнопки**

В `index.html` строка 120 — заменить:

```html
      <a href="#service" class="hero-cta-btn">РАССЧИТАТЬ СТОИМОСТЬ</a>
```

на:

```html
      <button type="button" class="hero-cta-btn" data-quiz-open>РАССЧИТАТЬ СТОИМОСТЬ</button>
```

- [ ] **Step 5: Smoke — страница открывается, кнопка не сломана**

Открыть `http://localhost:3050/`.
Ожидаемое:
- Страница рендерится без ошибок в консоли.
- Кнопка `РАССЧИТАТЬ СТОИМОСТЬ` визуально выглядит как раньше (CSS `.hero-cta-btn` уже задаёт padding/background/etc.; `<button>` подхватит те же стили — проверь визуально, шрифт может слегка отличаться из-за дефолтного браузерного стиля кнопки).
- Клик по кнопке пока ничего не делает (нет обработчика — это OK).
- В консоли никаких 404 на `quiz.css` или `quiz.js`.

Если у `<button>` визуально съехал шрифт/размер — добавь в `css/quiz.css` сброс:
```css
button.hero-cta-btn { font-family: inherit; border: none; cursor: pointer; }
```

- [ ] **Step 6: Коммит**

```bash
git add index.html css/quiz.css
git commit -m "feat(quiz): wire quiz.js/quiz.css into index.html, convert hero CTA to button"
```

---

## Task 5: Скелет модалки + open/close

**Files:**
- Modify: `js/quiz.js`
- Modify: `css/quiz.css`

- [ ] **Step 1: Добавить функции `ensureDialog`, `openQuiz`, `closeQuiz`, `render`, init-листенер в `js/quiz.js`**

Вставить **после** объявления `state` и **до** `window.__quiz = {...}`:

```js
  const DIALOG_ID = 'quizModal';

  function ensureDialog() {
    let dlg = document.getElementById(DIALOG_ID);
    if (dlg) return dlg;
    dlg = document.createElement('dialog');
    dlg.id = DIALOG_ID;
    dlg.className = 'quiz-modal';
    dlg.innerHTML = `
      <button type="button" class="quiz-close" aria-label="Закрыть" data-quiz-close>×</button>
      <div class="quiz-header">
        <h2 class="quiz-title">РАССЧИТАТЬ СТОИМОСТЬ</h2>
        <ul class="quiz-bait">
          <li>до 10% выгоды в смете при заключении договора этим летом</li>
          <li>3 месяца обслуживание бассейна бесплатно</li>
        </ul>
      </div>
      <div class="quiz-body" data-quiz-body></div>
    `;
    document.body.appendChild(dlg);

    // Закрытие по клику на бэкдроп (target === сам dialog)
    dlg.addEventListener('click', (e) => {
      if (e.target === dlg) closeQuiz();
    });
    // Кнопка ×
    dlg.addEventListener('click', (e) => {
      if (e.target.closest('[data-quiz-close]')) closeQuiz();
    });
    // Сброс state при закрытии (в т.ч. ESC и close-метод)
    dlg.addEventListener('close', () => { state = makeInitialState(); });

    return dlg;
  }

  function openQuiz() {
    const dlg = ensureDialog();
    state = makeInitialState();
    render();
    try { dlg.showModal(); }
    catch (err) {
      console.error('[quiz] showModal failed', err);
      alert('Ваш браузер не поддерживает эту форму, обновите его.');
    }
  }

  function closeQuiz() {
    const dlg = document.getElementById(DIALOG_ID);
    if (dlg && dlg.open) dlg.close();
  }

  function render() {
    const body = document.querySelector('[data-quiz-body]');
    if (!body) return;
    body.innerHTML = `<p style="padding:24px 0;text-align:center;color:#888;">Шаг ${state.step} из ${TOTAL_STEPS} (заглушка — будет в следующих задачах)</p>`;
  }

  document.addEventListener('DOMContentLoaded', () => {
    document.querySelectorAll('[data-quiz-open]').forEach((el) => {
      el.addEventListener('click', (e) => {
        e.preventDefault();
        openQuiz();
      });
    });
  });
```

Также **обновить** `window.__quiz` (в самом конце IIFE) — добавить dev-хуки:

```js
  window.__quiz = {
    normalizePhone,
    validateStep,
    _dev: {
      state: () => state,
      gotoStep: (n) => { state.step = n; render(); },
      open: openQuiz,
      close: closeQuiz
    }
  };
```

- [ ] **Step 2: Добавить базовые стили модалки в `css/quiz.css`**

Заменить содержимое (или дописать к существующему `button.hero-cta-btn` reset из Task 4):

```css
/* css/quiz.css */

/* Hero button reset (на случай если нужен после смены тега) */
button.hero-cta-btn { font-family: inherit; border: none; cursor: pointer; }

/* Modal */
.quiz-modal {
  border: none;
  padding: 0;
  background: #fff;
  color: #0f1117;
  max-width: 640px;
  width: calc(100vw - 32px);
  border-radius: 14px;
  box-shadow: 0 24px 60px rgba(0, 0, 0, 0.35);
  font-family: 'Montserrat', 'Segoe UI', sans-serif;
}
.quiz-modal::backdrop { background: rgba(15, 17, 23, 0.55); }

.quiz-close {
  position: absolute;
  top: 10px;
  right: 12px;
  width: 36px;
  height: 36px;
  border: none;
  background: transparent;
  font-size: 28px;
  line-height: 1;
  cursor: pointer;
  color: #555;
}
.quiz-close:hover { color: #000; }

.quiz-header { padding: 28px 28px 0; }
.quiz-title {
  margin: 0 0 14px;
  font-size: 18px;
  font-weight: 700;
  letter-spacing: 2px;
  text-transform: uppercase;
}
.quiz-bait {
  list-style: none;
  margin: 0 0 8px;
  padding: 0;
  display: flex;
  flex-direction: column;
  gap: 6px;
  font-size: 13px;
  color: #555;
}
.quiz-bait li::before { content: '✓ '; color: #0ea5e9; font-weight: 700; }

.quiz-body { padding: 16px 28px 28px; min-height: 240px; position: relative; }
```

- [ ] **Step 3: Smoke — модалка открывается и закрывается**

Открыть `http://localhost:3050/`, перезагрузить (Ctrl+Shift+R, чтобы сбросить кэш CSS), кликнуть `РАССЧИТАТЬ СТОИМОСТЬ`.

Ожидаемое:
- Модалка появляется по центру с тёмным бэкдропом.
- Заголовок «РАССЧИТАТЬ СТОИМОСТЬ» и две приманки видны.
- Внутри текст «Шаг 1 из 7 (заглушка — будет в следующих задачах)».
- `Esc` закрывает модалку.
- Клик по тёмному фону закрывает модалку.
- Клик по `×` закрывает модалку.
- Повторное открытие — снова шаг 1 (см. в DevTools: `__quiz._dev.state()` → `step: 1`).

- [ ] **Step 4: Коммит**

```bash
git add js/quiz.js css/quiz.css
git commit -m "feat(quiz): add dialog scaffold with open/close + base styles"
```

---

## Task 6: Рендер single-выбор (шаги 1, 2, 3, 5, 6)

**Files:**
- Modify: `js/quiz.js`
- Modify: `css/quiz.css`

- [ ] **Step 1: Реализовать `renderSingle` и обновить диспатчер**

В `js/quiz.js` **заменить** функцию `render` и добавить новые функции:

```js
  function renderSingle(step) {
    const selected = state.answers[step.id];
    return `
      <h3 class="quiz-step-title">${step.title}</h3>
      <div class="quiz-error" data-quiz-error></div>
      <div class="quiz-options">
        ${step.options.map(opt => `
          <button type="button"
                  class="quiz-option ${selected === opt.value ? 'is-active' : ''}"
                  data-quiz-pick="${opt.value}">${opt.label}</button>
        `).join('')}
      </div>
    `;
  }

  function render() {
    const body = document.querySelector('[data-quiz-body]');
    if (!body) return;
    const step = STEPS[state.step - 1];
    if (step.type === 'single') {
      body.innerHTML = renderSingle(step);
    } else {
      body.innerHTML = `<p style="padding:24px 0;text-align:center;color:#888;">Шаг ${state.step} (тип ${step.type} — будет в следующих задачах)</p>`;
    }
  }

  function onBodyClick(e) {
    const pick = e.target.closest('[data-quiz-pick]');
    if (pick) {
      const step = STEPS[state.step - 1];
      if (step.type === 'single') {
        state.answers[step.id] = pick.getAttribute('data-quiz-pick');
        render();
      }
    }
  }
```

В `ensureDialog`, **после** `document.body.appendChild(dlg);`, добавить:

```js
    dlg.querySelector('[data-quiz-body]').addEventListener('click', onBodyClick);
```

- [ ] **Step 2: Стили карточек-вариантов в `css/quiz.css`**

Дописать в конец:

```css
.quiz-step-title {
  margin: 0 0 12px;
  font-size: 18px;
  font-weight: 600;
}
.quiz-error {
  color: #c0392b;
  font-size: 13px;
  min-height: 20px;
  margin-bottom: 8px;
}
.quiz-options {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(160px, 1fr));
  gap: 10px;
}
.quiz-option {
  padding: 14px 12px;
  border: 1.5px solid #d8dde3;
  background: #fff;
  border-radius: 10px;
  cursor: pointer;
  font: inherit;
  font-size: 14px;
  text-align: center;
  color: #0f1117;
  transition: border-color 0.15s, background 0.15s;
}
.quiz-option:hover { border-color: #0ea5e9; }
.quiz-option.is-active {
  border-color: #0ea5e9;
  background: #eaf6fc;
  color: #0c4a6e;
}
```

- [ ] **Step 3: Smoke — single-шаги работают**

Открыть `http://localhost:3050/`, кликнуть `РАССЧИТАТЬ СТОИМОСТЬ`.

Ожидаемое:
- Шаг 1: видны 6 карточек с размерами.
- Клик по карточке — она подсвечивается голубой рамкой/фоном.
- Клик по другой карточке — подсветка переходит на неё (single-выбор).
- В DevTools: `__quiz._dev.state().answers.size` === значение выбранной карточки (например `'4x8'`).
- `__quiz._dev.gotoStep(2)` — рендерится шаг «Тип отделки», 3 варианта работают так же.
- `__quiz._dev.gotoStep(3)`, `5`, `6` — single-шаги рендерятся и кликаются.
- `__quiz._dev.gotoStep(4)` и `7` — пока заглушка «тип multi/contacts — будет в следующих задачах».

- [ ] **Step 4: Коммит**

```bash
git add js/quiz.js css/quiz.css
git commit -m "feat(quiz): render single-choice steps with option cards"
```

---

## Task 7: Рендер multi-выбор (шаг 4 — доп. опции)

**Files:**
- Modify: `js/quiz.js`

- [ ] **Step 1: Добавить `renderMulti` и расширить `onBodyClick`**

В `js/quiz.js` **добавить** функцию `renderMulti` рядом с `renderSingle`:

```js
  function renderMulti(step) {
    const selected = state.answers[step.id] || [];
    return `
      <h3 class="quiz-step-title">${step.title}</h3>
      <div class="quiz-error" data-quiz-error></div>
      <div class="quiz-options">
        ${step.options.map(opt => `
          <button type="button"
                  class="quiz-option ${selected.includes(opt.value) ? 'is-active' : ''}"
                  data-quiz-toggle="${opt.value}">${opt.label}</button>
        `).join('')}
      </div>
    `;
  }
```

**Обновить диспатчер** `render`:

```js
  function render() {
    const body = document.querySelector('[data-quiz-body]');
    if (!body) return;
    const step = STEPS[state.step - 1];
    if (step.type === 'single')      body.innerHTML = renderSingle(step);
    else if (step.type === 'multi')  body.innerHTML = renderMulti(step);
    else body.innerHTML = `<p style="padding:24px 0;text-align:center;color:#888;">Шаг ${state.step} (тип ${step.type} — будет в следующих задачах)</p>`;
  }
```

**Расширить** `onBodyClick`:

```js
  function onBodyClick(e) {
    const pick = e.target.closest('[data-quiz-pick]');
    if (pick) {
      const step = STEPS[state.step - 1];
      if (step.type === 'single') {
        state.answers[step.id] = pick.getAttribute('data-quiz-pick');
        render();
      }
      return;
    }
    const toggle = e.target.closest('[data-quiz-toggle]');
    if (toggle) {
      const step = STEPS[state.step - 1];
      const value = toggle.getAttribute('data-quiz-toggle');
      const arr = state.answers[step.id];
      const idx = arr.indexOf(value);
      if (idx >= 0) arr.splice(idx, 1); else arr.push(value);
      render();
      return;
    }
  }
```

- [ ] **Step 2: Smoke — multi-выбор на шаге 4**

Перезагрузить страницу, открыть квиз, в DevTools: `__quiz._dev.gotoStep(4)`.

Ожидаемое:
- Видны 7 карточек (Подогрев воды … Водопад).
- Клик по карточке — подсвечивается. Клик ещё раз — снимается.
- Можно выбрать несколько одновременно.
- `__quiz._dev.state().answers.options` — массив `['heating', 'lighting', ...]` в порядке клика.

- [ ] **Step 3: Коммит**

```bash
git add js/quiz.js
git commit -m "feat(quiz): render multi-choice step (additional options)"
```

---

## Task 8: Рендер шага «Контакты» (шаг 7)

**Files:**
- Modify: `js/quiz.js`
- Modify: `css/quiz.css`

- [ ] **Step 1: Добавить `renderContacts`, `escapeHtml`, `onBodyInput`**

В `js/quiz.js` **добавить**:

```js
  function escapeHtml(s) {
    return String(s == null ? '' : s)
      .replace(/&/g, '&amp;')
      .replace(/</g, '&lt;')
      .replace(/>/g, '&gt;')
      .replace(/"/g, '&quot;');
  }

  function renderContacts(step) {
    return `
      <h3 class="quiz-step-title">${step.title}</h3>
      <div class="quiz-error" data-quiz-error></div>
      <div class="quiz-fields">
        <input type="text" class="quiz-input"
               data-quiz-field="name"
               placeholder="Имя"
               value="${escapeHtml(state.answers.name)}"
               autocomplete="given-name">
        <input type="tel" class="quiz-input"
               data-quiz-field="phone"
               placeholder="Телефон"
               value="${escapeHtml(state.answers.phone)}"
               autocomplete="tel">
      </div>
      <p class="quiz-consent">Нажимая на кнопку вы соглашаетесь на обработку данных</p>
    `;
  }

  function onBodyInput(e) {
    const target = e.target;
    if (!target || !target.getAttribute) return;
    const field = target.getAttribute('data-quiz-field');
    if (!field) return;
    state.answers[field] = target.value;
    // Не делаем render() — потеряли бы фокус и курсор
  }
```

**Обновить диспатчер** `render`:

```js
  function render() {
    const body = document.querySelector('[data-quiz-body]');
    if (!body) return;
    const step = STEPS[state.step - 1];
    if (step.type === 'single')         body.innerHTML = renderSingle(step);
    else if (step.type === 'multi')     body.innerHTML = renderMulti(step);
    else if (step.type === 'contacts')  body.innerHTML = renderContacts(step);
  }
```

В `ensureDialog`, **после** строки с `addEventListener('click', onBodyClick)`, добавить:

```js
    dlg.querySelector('[data-quiz-body]').addEventListener('input', onBodyInput);
```

- [ ] **Step 2: Стили формы в `css/quiz.css`**

Дописать в конец:

```css
.quiz-fields {
  display: flex;
  flex-direction: column;
  gap: 10px;
}
.quiz-input {
  padding: 14px 16px;
  border: 1.5px solid #d8dde3;
  border-radius: 10px;
  font: inherit;
  font-size: 15px;
  color: #0f1117;
}
.quiz-input::placeholder { color: #9aa1ab; }
.quiz-input:focus {
  border-color: #0ea5e9;
  outline: none;
}
.quiz-consent {
  font-size: 12px;
  color: #888;
  margin: 12px 0 0;
}
```

- [ ] **Step 3: Smoke — шаг 7**

Перезагрузить, открыть квиз, `__quiz._dev.gotoStep(7)`.

Ожидаемое:
- Заголовок «Контакты», два инпута с плейсхолдерами `Имя` и `Телефон`.
- Под формой строка `Нажимая на кнопку вы соглашаетесь на обработку данных`.
- Печатаешь в инпуты — фокус не теряется, курсор на месте.
- `__quiz._dev.state().answers.name` и `.phone` отражают введённое.

- [ ] **Step 4: Коммит**

```bash
git add js/quiz.js css/quiz.css
git commit -m "feat(quiz): render contacts step with name/phone inputs"
```

---

## Task 9: Кнопки «Назад / Далее» + проброс валидации

**Files:**
- Modify: `js/quiz.js`
- Modify: `css/quiz.css`

- [ ] **Step 1: Добавить `renderNav`, `showError`, навигационные ветки в `onBodyClick`**

В `js/quiz.js` **добавить**:

```js
  function renderNav() {
    const isLast = state.step === TOTAL_STEPS;
    const showBack = state.step > 1;
    const nextLabel = isLast ? 'ПОЛУЧИТЬ ПРЕДЛОЖЕНИЕ' : 'ДАЛЕЕ →';
    const nextAttr  = isLast ? 'data-quiz-submit' : 'data-quiz-next';
    return `
      <div class="quiz-nav">
        <button type="button" class="quiz-btn quiz-btn--back" data-quiz-back ${showBack ? '' : 'hidden'}>← НАЗАД</button>
        <button type="button" class="quiz-btn quiz-btn--next" ${nextAttr}>${nextLabel}</button>
      </div>
    `;
  }

  function showError(msg) {
    const el = document.querySelector('[data-quiz-error]');
    if (el) el.textContent = msg;
  }
```

**Обновить** `render` — добавить `renderNav()` к каждой ветке:

```js
  function render() {
    const body = document.querySelector('[data-quiz-body]');
    if (!body) return;
    const step = STEPS[state.step - 1];
    let inner = '';
    if (step.type === 'single')        inner = renderSingle(step);
    else if (step.type === 'multi')    inner = renderMulti(step);
    else if (step.type === 'contacts') inner = renderContacts(step);
    body.innerHTML = inner + renderNav();
  }
```

**Расширить** `onBodyClick` — добавить ветки `back` и `next` **в начало** функции (до pick/toggle):

```js
  function onBodyClick(e) {
    if (e.target.closest('[data-quiz-back]')) {
      if (state.step > 1) { state.step--; render(); }
      return;
    }
    if (e.target.closest('[data-quiz-next]')) {
      const v = validateStep(state.step, state.answers);
      if (!v.ok) { showError(v.message); return; }
      state.step++;
      render();
      return;
    }
    // submit обработаем в Task 10

    const pick = e.target.closest('[data-quiz-pick]');
    if (pick) {
      const step = STEPS[state.step - 1];
      if (step.type === 'single') {
        state.answers[step.id] = pick.getAttribute('data-quiz-pick');
        render();
      }
      return;
    }
    const toggle = e.target.closest('[data-quiz-toggle]');
    if (toggle) {
      const step = STEPS[state.step - 1];
      const value = toggle.getAttribute('data-quiz-toggle');
      const arr = state.answers[step.id];
      const idx = arr.indexOf(value);
      if (idx >= 0) arr.splice(idx, 1); else arr.push(value);
      render();
      return;
    }
  }
```

**Обновить** `onBodyInput` — очищать ошибку при вводе:

```js
  function onBodyInput(e) {
    const target = e.target;
    if (!target || !target.getAttribute) return;
    const field = target.getAttribute('data-quiz-field');
    if (!field) return;
    state.answers[field] = target.value;
    showError('');
  }
```

- [ ] **Step 2: Стили навигации в `css/quiz.css`**

Дописать в конец:

```css
.quiz-nav {
  display: flex;
  align-items: center;
  gap: 12px;
  margin-top: 24px;
}
.quiz-btn {
  padding: 14px 26px;
  border: none;
  border-radius: 10px;
  font: inherit;
  font-size: 14px;
  font-weight: 700;
  letter-spacing: 1px;
  cursor: pointer;
  transition: transform 0.2s, box-shadow 0.2s, background 0.2s;
}
.quiz-btn--back {
  background: transparent;
  color: #555;
  padding-left: 0;
}
.quiz-btn--back:hover { color: #000; }
.quiz-btn--next {
  margin-left: auto;
  background: linear-gradient(135deg, #0ea5e9, #0284c7);
  color: #fff;
  box-shadow: 0 6px 20px rgba(14, 165, 233, 0.25);
}
.quiz-btn--next:hover {
  transform: translateY(-2px);
  box-shadow: 0 8px 28px rgba(14, 165, 233, 0.35);
}
.quiz-btn[hidden] { display: none; }
```

- [ ] **Step 3: Smoke — навигация работает**

Перезагрузить, кликнуть `РАССЧИТАТЬ СТОИМОСТЬ`.

Ожидаемое:
- На шаге 1: кнопка `← НАЗАД` скрыта, видна `ДАЛЕЕ →`.
- Без выбора клик `ДАЛЕЕ →` — появляется красная строка `Выберите размер`, шаг не сменился.
- Выбор любой карточки — клик `ДАЛЕЕ →` ведёт на шаг 2.
- На шаге 2 видна `← НАЗАД`, клик возвращает на шаг 1, прежний выбор подсвечен.
- Шаг 4: `ДАЛЕЕ →` работает даже без выбора (multi optional).
- На шаге 7: вместо `ДАЛЕЕ →` — `ПОЛУЧИТЬ ПРЕДЛОЖЕНИЕ`. Клик пока ничего не делает (Task 10).
- Шаг 7 без имени — должно показывать ошибку при клике `ПОЛУЧИТЬ ПРЕДЛОЖЕНИЕ`. Эта ветка появится в Task 10. Сейчас просто проверь, что добраться до шага 7 можно.

- [ ] **Step 4: Коммит**

```bash
git add js/quiz.js css/quiz.css
git commit -m "feat(quiz): wire back/next navigation with per-step validation"
```

---

## Task 10: Submit-заглушка + экран благодарности

**Files:**
- Modify: `js/quiz.js`
- Modify: `css/quiz.css`

- [ ] **Step 1: Добавить `handleSubmit`, `showThankYou`, ветку submit в `onBodyClick`**

В `js/quiz.js` **добавить**:

```js
  function handleSubmit() {
    const v = validateStep(state.step, state.answers);
    if (!v.ok) { showError(v.message); return; }

    console.log('[quiz] submit', state.answers);
    // TODO: replace with POST /api/leads when backend is ready

    showThankYou();
  }

  function showThankYou() {
    const body = document.querySelector('[data-quiz-body]');
    if (!body) return;
    body.innerHTML = `
      <div class="quiz-thanks">
        <h3 class="quiz-thanks-title">Спасибо! Заявка принята</h3>
        <p class="quiz-thanks-text">Менеджер свяжется с вами в течение 10 минут</p>
        <button type="button" class="quiz-btn quiz-btn--next" data-quiz-close>Закрыть</button>
      </div>
    `;
  }
```

**Заменить** в `onBodyClick` строку `// submit обработаем в Task 10` на:

```js
    if (e.target.closest('[data-quiz-submit]')) {
      handleSubmit();
      return;
    }
```

- [ ] **Step 2: Стили экрана благодарности в `css/quiz.css`**

Дописать в конец:

```css
.quiz-thanks {
  text-align: center;
  padding: 16px 0 8px;
}
.quiz-thanks-title {
  margin: 0 0 10px;
  font-size: 22px;
  font-weight: 700;
}
.quiz-thanks-text {
  color: #555;
  margin: 0 0 24px;
  font-size: 15px;
}
.quiz-thanks .quiz-btn--next {
  margin: 0 auto;
}
```

- [ ] **Step 3: Smoke — полный e2e**

Перезагрузить, кликнуть `РАССЧИТАТЬ СТОИМОСТЬ`. Пройти все 7 шагов:
1. Размер → выбрать `4 × 8 м` → `ДАЛЕЕ →`.
2. Отделка → `Композитный` → `ДАЛЕЕ →`.
3. Фильтрация → `Да` → `ДАЛЕЕ →`.
4. Опции → выбрать 2-3 опции → `ДАЛЕЕ →`.
5. Бюджет → `2–3 млн руб` → `ДАЛЕЕ →`.
6. Сроки → `В ближайшее время` → `ДАЛЕЕ →`.
7. Имя `Иван`, Телефон `+7 999 123-45-67` → `ПОЛУЧИТЬ ПРЕДЛОЖЕНИЕ`.

Ожидаемое:
- В консоли: `[quiz] submit { size: '4x8', finish: 'composite', filtration: 'yes', options: [...], budget: '2-3', timing: 'soon', name: 'Иван', phone: '+7 999 123-45-67' }`.
- Появился экран `Спасибо! Заявка принята / Менеджер свяжется с вами в течение 10 минут / [Закрыть]`.
- Клик `Закрыть` — модалка закрывается.
- Повторно кликаем `РАССЧИТАТЬ СТОИМОСТЬ` — снова шаг 1, поля чистые (state сбросился).

Также проверить ошибки на шаге 7:
- На шаге 7 имя `И` (1 символ) → клик `ПОЛУЧИТЬ ПРЕДЛОЖЕНИЕ` → красная строка `Введите имя`, экран не сменился.
- Имя `Иван`, телефон `123` → `ПОЛУЧИТЬ ПРЕДЛОЖЕНИЕ` → `Введите телефон`.
- Печать в инпут любого символа очищает ошибку.

- [ ] **Step 4: Коммит**

```bash
git add js/quiz.js css/quiz.css
git commit -m "feat(quiz): add submit stub and thank-you screen"
```

---

## Task 11: Адаптив для мобилки

**Files:**
- Modify: `css/quiz.css`

- [ ] **Step 1: Добавить media-query в конец `css/quiz.css`**

```css
@media (max-width: 480px) {
  .quiz-modal {
    width: calc(100vw - 16px);
    max-height: calc(100vh - 32px);
    overflow-y: auto;
  }
  .quiz-header { padding: 24px 20px 0; }
  .quiz-body { padding: 14px 20px 24px; }
  .quiz-title { font-size: 16px; }
  .quiz-options { grid-template-columns: 1fr; }
  .quiz-nav { flex-direction: column-reverse; align-items: stretch; gap: 8px; }
  .quiz-btn--next { margin-left: 0; }
  .quiz-btn--back { padding: 12px 0; text-align: center; }
}
```

- [ ] **Step 2: Smoke — мобильный размер**

В DevTools включить toggle device toolbar (Ctrl+Shift+M), выбрать iPhone SE (375×667) или просто width=375.

Перезагрузить, открыть квиз. Пройти 1-2 шага.

Ожидаемое:
- Модалка занимает почти всю ширину, не вылезает за границы.
- Карточки-варианты идут одной колонкой.
- Кнопка `ДАЛЕЕ →` на всю ширину, `← НАЗАД` под ней.
- Можно прокрутить модалку вертикально, если контент длинный.
- Поля ввода имени/телефона помещаются и читаемы.

- [ ] **Step 3: Коммит**

```bash
git add css/quiz.css
git commit -m "style(quiz): mobile responsive (≤480px)"
```

---

## Task 12: Полный smoke-чеклист из спеки

**Files:** только проверка, фиксы по итогам.

- [ ] **Step 1: Прогнать чеклист из спеки**

Перезагрузить (Ctrl+Shift+R) и пройти весь список. Каждый пункт — отметить (галочка/крестик):

- [ ] Клик `РАССЧИТАТЬ СТОИМОСТЬ` → модалка открывается на шаге 1.
- [ ] Без выбора клик `ДАЛЕЕ` → ошибка «Выберите размер», шаг не сменился.
- [ ] Выбор → ошибка ушла → `ДАЛЕЕ` ведёт на шаг 2.
- [ ] `НАЗАД` с шага 2 возвращает выбор шага 1 (state сохранён в сессии).
- [ ] Шаг 4 — выбор нескольких опций работает (toggle).
- [ ] Шаг 4 — пустой выбор пропускает дальше без ошибки.
- [ ] Шаг 7 — невалидный телефон (`abc`) → ошибка `Введите телефон`.
- [ ] Submit с валидными полями → `console.log` с `state.answers`, экран благодарности.
- [ ] Кнопка `Закрыть` на финале и `Esc` закрывают модалку.
- [ ] Повторное открытие — с шага 1, поля пустые.
- [ ] Мобильная ширина (DevTools, 375 px) — модалка влазит, кнопки доступны.
- [ ] Существующая форма `#service` не сломана. Ссылки `Услуги` и `Оставить заявку` в мобильном меню и `Оставить заявку` в футере по-прежнему скроллят к секции `#service` и открывают форму записи на сервис.
- [ ] `js/quiz.test.html` — все 26 ассертов зелёные.

- [ ] **Step 2: Если что-то упало — починить и закоммитить отдельным коммитом**

```bash
git add <files>
git commit -m "fix(quiz): <что починили>"
```

- [ ] **Step 3: Если всё зелёное — финального коммита не нужно**

Просто отчитаться: «Smoke-чеклист зелёный, фиксов не потребовалось».

---

## Что в скоуп НЕ входит (для напоминания)

- Бэкенд: миграция БД с таблицей `quiz_leads`, маршрут `POST /api/leads`, страница `/admin/leads`, уведомления (Telegram / SMTP). Маркер `// TODO: replace with POST /api/leads when backend is ready` стоит в коде на месте submit.
- Сохранение прогресса между сессиями (localStorage).
- Маска ввода телефона.
- Прогресс-бар «Шаг N из 7» (на референсе нет, держим 1:1).
- Расчёт ориентировочной цены на финальном экране.
- Открытие квиза из других CTA-кнопок (`Оставить заявку` в меню/футере, `Бесплатная консультация`).
